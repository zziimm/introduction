import React from 'react';
import { styled } from 'styled-components';

const CareerInner = styled.div`
  background: #fff;
  height: 300px;
`;

function Skills(props) {
  return (
    <CareerInner>
      
    </CareerInner>
  );
}

export default Skills;